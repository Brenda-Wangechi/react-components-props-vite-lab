import React from "react";

// Home component: high-level page container that would compose `Header`,
// `About`, and `ArticleList`. Provided for organization and future use.
// Connected to: none directly in this lab — `App` currently composes children.
function Home() {
  return <main>{/* Compose Header, About, ArticleList here if used */}</main>;
}

export default Home;
